Autosave:
	every X minutes or X user commands?
	map too?

/save without arguments makes a timestamped backup
/load without arguments loads the last backup using ctime()

Normalize motion commands ("up" -> "u", "down"->"d")

/drive like /nav but executes commands

